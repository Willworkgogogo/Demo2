// function hello<T>(name: T): T {
//     return name
// }
// let student1 = hello('wang')
function logging(arg) {
    console.log(arg.length);
    return arg;
}
function logging2(arg) {
    console.log(arg.length);
    return arg;
}
